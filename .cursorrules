# 项目：OpenChart Pro

## 项目简介

加密货币与股票行情分析平台。后端 Python FastAPI，前端原生 HTML/JS，集成缠论、艾略特波浪、斐波那契等技术分析工具，支持 OKX/Binance/Yahoo/东方财富多市场数据。

## 技术栈

- 后端：Python 3.12+ / FastAPI / Uvicorn / WebSocket / aiosqlite
- 前端：原生 HTML + CSS + JS（无框架）/ KLineCharts v9 / Chart.js
- 数据库：SQLite（data/openchart.db）
- 数据源：OKX API / Binance API / Yahoo Finance / 东方财富

## 目录结构

```
run.py                    # 一键启动（python run.py）
backend/
  main.py                 # FastAPI 主入口（所有路由）
  config.py               # 默认配置模板
  chanlun_engine/         # 缠论引擎（直接引用 chan.py 源码）
    chanlun_service.py    # 我们写的适配层（唯一改动文件）
  elliott_wave/           # 艾略特波浪模块
    service.py            # 分析服务
    detector.py           # 检测引擎
  data/                   # 数据源适配器（okx.py/binance.py/yahoo.py）
  indicators/             # 技术指标
  trading/                # 实盘交易
  backtest/               # 回测引擎
  ws/                     # WebSocket Hub
frontend/
  index.html              # 单页应用入口
  js/chart.js             # K线图核心（缠论/艾略特/斐波那契渲染）
  js/app.js               # 应用初始化
  css/app.css             # 样式
  lib/                    # 第三方 JS 库（本地引入）
```

## 代码风格

- Python：有 docstring 和类型注解，无格式化工具
- JS：无框架，全局函数 + window 变量通信
- 注释语言：全部使用中文（代码注释、docstring、commit message）
- 变量命名：Python 用下划线（snake_case），JS 用驼峰（camelCase）
- 前端缓存刷新：修改 index.html 的 `?v=N` 版本号

## 常用命令

- 启动：`python run.py`（端口 8888）
- 或：`python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000`
- 无测试命令（项目暂无测试）

## 关键注意事项

### 缠论模块
- `chanlun_engine/` 是 chan.py 第三方引擎的**完整源码**，不要修改里面的文件
- 我们只改 `chanlun_engine/chanlun_service.py`（适配层）
- **时区坑**：`_ts_to_ctime` 必须用 `datetime.fromtimestamp`（本地时间），不能用 `utcfromtimestamp`，否则所有指标偏移 8 根 K 线
- 参数自适应：K 线 ≤500 根用宽松中枢（one_bi_zs=True），>500 根用严格标准

### 前端 K 线图
- chart.js 的缓存版本号在 index.html 里（`?v=N`），每次改 chart.js 必须递增
- 缠论绘制用 KLineCharts 的自定义指标 overlay（canvas 直接画）
- loadMore 加载历史 K 线后会自动重新运行缠论分析

### 数据流
- 前端 `loadChanlun()` → 发送全量 K 线到后端 → `chanlun_service.analyze()` → 返回笔/线段/中枢/买卖点坐标 → 前端 canvas 渲染

## 与用户协作的规则

- 用户是非技术人员，所有解释用中文，假设用户没有编程基础
- 改代码前先告诉用户准备改什么、为什么
- 不要自作主张装新依赖，先问
- 每次改完代码告诉用户怎么验证
- 发现 bug 时，建议用户把教训写进 docs/ai/lessons.md
- 遇到敏感信息或破坏性操作，立即停止并告警
- 每次用户说"我要提交代码"时，主动提醒他对照 docs/checklist.md 检查
- 修改本地代码后不要自动部署到服务器，等用户指示
